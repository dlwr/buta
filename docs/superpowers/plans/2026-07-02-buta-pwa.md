# buta PWA (トリアージ UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 同一 Worker から静的配信する PWA。`GET /feed` の二層を **1本のリスト**（Tier 1 → 継ぎ目 → Tier 2）で描画し、スクロール通過を `POST /viewed` にバッチ報告（自動既読＋回転）。j/k/s/o キー、スター、手動掃除（`POST /cleanup`、尾のみ・明示操作）を備える。

**Architecture:** Workers Static Assets（`wrangler.jsonc` の `assets`）で `public/` を同一オリジン配信 → CORS 不要、トークンは localStorage。フロントは **フレームワークなしの素の TS 1 ファイル**を esbuild でバンドル。テスト可能なロジック（ViewedBuffer）は純クラスに分離して TDD、DOM グルーは手動検証。

**Tech Stack:** 既存スタック + esbuild（devDependency）。DOM 用に `src/pwa/tsconfig.json` を分離（workers-types と DOM lib は同一プログラムに共存できない）。

## Global Constraints

- PWA は API と**同一オリジン**（アセット優先ルーティング。API パスはファイルと衝突しないので Worker に落ちる）。
- 認証: localStorage の `buta_token` を `Authorization: Bearer` で送出。401 なら prompt で再入力。
- **未読カウントを UI に出さない**（数字で急かさない）。
- 自動既読は**ビューポート上端を通過した行のみ**。報告は 4 秒毎＋`pagehide` でフラッシュ（`fetch keepalive`）。
- `POST /cleanup` は **Tier 2（尾）のみ**対象、`olderThanDays` 必須、明示操作でしか呼ばれない。`last_surfaced` は触らない（見ていないので）。
- デザイントークン: 地 `#FAF7F5`/文字 `#23201E`/薄文字 `#8A817C`/アクセント（豚ローズ）`#C4526B`、ダークは地 `#1B171B`/文字 `#E8E2DE`/アクセント `#E27A93`。本文 system-ui、メタ ui-monospace。シグネチャは Tier 継ぎ目のみ。既読行は opacity 0.38。
- コミットはタスクごと。ロジックは TDD。

---

### Task 1: 静的アセット配信のスキャフォールド

**Files:**
- Modify: `package.json`（esbuild 追加、`build:pwa`、deploy 前ビルド）
- Modify: `wrangler.jsonc`（`assets`）
- Create: `public/index.html`（プレースホルダ）
- Create: `src/pwa/tsconfig.json`
- Modify: `tsconfig.json`（`src/pwa` を exclude）

**Interfaces:**
- Produces: `pnpm build:pwa` が `src/pwa/app.ts` → `public/app.js` をバンドル。`/` で index.html が配信される（本番）。既存 API テストは無影響（worker.fetch 直呼びはアセット層を通らない）。

- [ ] **Step 1: package.json を更新**

devDependencies に `"esbuild": "^0.24.0"` を追加し、scripts を：

```json
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc -p src/pwa --noEmit",
    "build:pwa": "esbuild src/pwa/app.ts --bundle --outfile=public/app.js --format=iife --minify",
    "deploy": "pnpm build:pwa && wrangler deploy"
  }
```

`pnpm.onlyBuiltDependencies` に esbuild は既にある。`pnpm install` を実行。

- [ ] **Step 2: wrangler.jsonc に assets を追加**

```jsonc
  "assets": { "directory": "./public" },
```

- [ ] **Step 3: public/index.html プレースホルダと src/pwa/app.ts の空実装**

```html
<!doctype html><meta charset="utf-8"><title>buta</title><p>buta</p>
```

```ts
// src/pwa/app.ts
console.log("buta");
```

- [ ] **Step 4: tsconfig 分離**

`src/pwa/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022", "module": "ES2022", "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true, "noUncheckedIndexedAccess": true,
    "skipLibCheck": true, "isolatedModules": true, "noEmit": true
  },
  "include": ["./**/*.ts"]
}
```

ルート `tsconfig.json` の `include` を `["src", "test"]` のまま、`"exclude": ["src/pwa"]` を追加。

- [ ] **Step 5: 検証 + コミット**

Run: `pnpm install && pnpm build:pwa && pnpm test && pnpm typecheck`
Expected: 全 PASS（`public/app.js` 生成）。もし vitest pool が assets 設定で失敗する場合は、`vitest.config.ts` の `miniflare` に `assets` を渡さない override を足す。

```bash
git add package.json pnpm-lock.yaml wrangler.jsonc public/index.html src/pwa/app.ts src/pwa/tsconfig.json tsconfig.json
git commit -m "chore(pwa): static assets scaffold with esbuild bundle"
```

`public/app.js` は生成物なので `.gitignore` に追加してもよい（deploy 時に必ずビルドされる）。追加する場合は `.gitignore` に `public/app.js`。

---

### Task 2: ViewedBuffer（純ロジック・TDD）

**Files:**
- Create: `src/pwa/viewed-buffer.ts`
- Test: `test/pwa/viewed-buffer.test.ts`

**Interfaces:**
- Produces: `class ViewedBuffer` — `add(id: number): boolean`（新規なら true。報告済み/保留中は false）、`drain(): number[]`（保留分を取り出して空にする。返した id は「報告済み」扱い）、`restore(ids: number[]): void`（送信失敗時に保留へ戻す）。

- [ ] **Step 1: 失敗するテストを書く**

```ts
// test/pwa/viewed-buffer.test.ts
import { describe, it, expect } from "vitest";
import { ViewedBuffer } from "../../src/pwa/viewed-buffer";

describe("ViewedBuffer", () => {
  it("collects new ids and drains them once", () => {
    const b = new ViewedBuffer();
    expect(b.add(1)).toBe(true);
    expect(b.add(1)).toBe(false);     // dedup while pending
    expect(b.add(2)).toBe(true);
    expect(b.drain()).toEqual([1, 2]);
    expect(b.drain()).toEqual([]);     // empty after drain
    expect(b.add(1)).toBe(false);      // already reported, never re-queued
  });

  it("restore puts failed ids back for the next drain", () => {
    const b = new ViewedBuffer();
    b.add(1); b.add(2);
    const batch = b.drain();
    b.restore(batch);                  // POST failed
    expect(b.drain()).toEqual([1, 2]); // retried next time
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm vitest run test/pwa/viewed-buffer.test.ts`
Expected: FAIL（モジュール未作成）

- [ ] **Step 3: 実装**

```ts
// src/pwa/viewed-buffer.ts
export class ViewedBuffer {
  private pending = new Set<number>();
  private reported = new Set<number>();

  add(id: number): boolean {
    if (this.pending.has(id) || this.reported.has(id)) return false;
    this.pending.add(id);
    return true;
  }

  drain(): number[] {
    const out = [...this.pending];
    for (const id of out) {
      this.pending.delete(id);
      this.reported.add(id);
    }
    return out;
  }

  restore(ids: number[]): void {
    for (const id of ids) {
      this.reported.delete(id);
      this.pending.add(id);
    }
  }
}
```

（注: このファイルは DOM 非依存なのでルート tsconfig からも test からも参照できる。`exclude: ["src/pwa"]` はルートの型検査から外すだけで、vitest はファイル単位で変換するため問題ない。）

- [ ] **Step 4: テストが通ることを確認 + コミット**

Run: `pnpm vitest run test/pwa/viewed-buffer.test.ts`
Expected: PASS

```bash
git add src/pwa/viewed-buffer.ts test/pwa/viewed-buffer.test.ts
git commit -m "feat(pwa): viewed buffer with dedup and failure restore"
```

---

### Task 3: POST /cleanup（尾の手動掃除・TDD）

**Files:**
- Modify: `src/index.ts`
- Test: `test/cleanup-endpoint.test.ts`

**Interfaces:**
- Consumes: `getUnreadForSelection`, `getAllTaggings`, `buildFeedTierMap`, `markEntriesRead`(Feedbin), `markEntriesReadLocal`, `DEFAULT_SELECTION_CONFIG`
- Produces: `POST /cleanup`（auth）body `{ "olderThanDays": number }` → Tier 2 の feed に属する未読のうち `created_at < now - days` のものを Feedbin で既読化 → ローカル反映 → `{ read: number }`。`olderThanDays` が正の数でなければ 400。対象ゼロなら Feedbin を呼ばず `{ read: 0 }`。**last_surfaced は更新しない**。

- [ ] **Step 1: 失敗するテストを書く**

```ts
// test/cleanup-endpoint.test.ts
import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
import { env } from "cloudflare:test";
import worker from "../src/index";
import { initSchema, getFeedLastSurfaced } from "../src/db/queries";

const AUTH = { Authorization: "Bearer test-token", "Content-Type": "application/json" };
const OLD = "2026-06-01T00:00:00Z";
const FRESH = "2026-07-02T00:00:00Z";

beforeEach(async () => {
  await initSchema(env.DB);
  await env.DB.exec("DELETE FROM entries");
  await env.DB.exec("DELETE FROM taggings");
  await env.DB.exec("DELETE FROM feed_state");
  await env.DB.batch([
    env.DB.prepare("INSERT INTO taggings (id, feed_id, name) VALUES (1, 1, 'Must Read')"),
    env.DB.prepare("INSERT INTO taggings (id, feed_id, name) VALUES (2, 2, 'Blog')"),
    // core feed, old -> must NOT be cleaned
    env.DB.prepare(`INSERT INTO entries (id, feed_id, title, url, created_at, is_unread, synced_at) VALUES (1, 1, 'core-old', null, '${OLD}', 1, 't')`),
    // tail feed, old -> cleaned
    env.DB.prepare(`INSERT INTO entries (id, feed_id, title, url, created_at, is_unread, synced_at) VALUES (2, 2, 'tail-old', null, '${OLD}', 1, 't')`),
    // tail feed, fresh -> kept
    env.DB.prepare(`INSERT INTO entries (id, feed_id, title, url, created_at, is_unread, synced_at) VALUES (3, 2, 'tail-fresh', null, '${FRESH}', 1, 't')`),
  ]);
});
afterEach(() => {
  vi.unstubAllGlobals();
});

function stubFeedbin() {
  const bodies: unknown[] = [];
  vi.stubGlobal("fetch", async (_url: string | URL, init?: RequestInit) => {
    const body = init?.body ? JSON.parse(String(init.body)) : {};
    bodies.push(body);
    return new Response(JSON.stringify(Object.values(body)[0] ?? []), { status: 200 });
  });
  return bodies;
}

function post(body: unknown) {
  return worker.fetch(
    new Request("https://buta.example/cleanup", {
      method: "POST", headers: AUTH, body: JSON.stringify(body),
    }),
    env as never, {} as never,
  );
}

describe("POST /cleanup", () => {
  it("marks only old tail entries read, never core, never fresh", async () => {
    const bodies = stubFeedbin();
    const res = await post({ olderThanDays: 7 });
    expect(res.status).toBe(200);
    expect(await res.json()).toEqual({ read: 1 });
    expect(bodies[0]).toEqual({ unread_entries: [2] });

    const rows = await env.DB.prepare(
      "SELECT id, is_unread FROM entries ORDER BY id",
    ).all();
    expect(rows.results).toEqual([
      { id: 1, is_unread: 1 },  // core untouched
      { id: 2, is_unread: 0 },  // old tail cleaned
      { id: 3, is_unread: 1 },  // fresh tail kept
    ]);
    // cleanup is not a "view": last_surfaced untouched
    expect((await getFeedLastSurfaced(env.DB)).size).toBe(0);
  });

  it("400 on missing/invalid olderThanDays", async () => {
    expect((await post({})).status).toBe(400);
    expect((await post({ olderThanDays: -1 })).status).toBe(400);
  });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `pnpm vitest run test/cleanup-endpoint.test.ts`
Expected: FAIL（`/cleanup` は 404）

- [ ] **Step 3: 実装**

`src/index.ts` の import に `buildFeedTierMap` を追加：

```ts
import { buildSelection, buildFeedTierMap, DEFAULT_SELECTION_CONFIG } from "./selection/select";
```

ルーティングに追加（`/star` の後）：

```ts
    if (request.method === "POST" && url.pathname === "/cleanup") {
      if (!isAuthorized(request, env.ADMIN_TOKEN)) {
        return new Response("Unauthorized", { status: 401 });
      }
      const body = (await request.json().catch(() => null)) as
        { olderThanDays?: unknown } | null;
      const days = body?.olderThanDays;
      if (typeof days !== "number" || !Number.isFinite(days) || days <= 0) {
        return new Response("Bad Request", { status: 400 });
      }
      const cutoff = new Date(Date.now() - days * 86_400_000).toISOString();

      const [entries, taggings] = await Promise.all([
        getUnreadForSelection(env.DB),
        getAllTaggings(env.DB),
      ]);
      const tierMap = buildFeedTierMap(taggings, DEFAULT_SELECTION_CONFIG.coreTags);
      const targets = entries
        .filter((e) => (tierMap.get(e.feed_id) ?? 2) === 2)
        .filter((e) => (e.created_at ?? "") < cutoff)
        .map((e) => e.id);
      if (targets.length === 0) return Response.json({ read: 0 });

      let confirmed: number[];
      try {
        confirmed = await makeFeedbinClient(env).markEntriesRead(targets);
      } catch {
        return new Response("Feedbin write failed", { status: 502 });
      }
      await markEntriesReadLocal(env.DB, confirmed);
      return Response.json({ read: confirmed.length });
    }
```

- [ ] **Step 4: 全テスト + 型チェック + コミット**

Run: `pnpm test && pnpm typecheck`
Expected: 全 PASS

```bash
git add src/index.ts test/cleanup-endpoint.test.ts
git commit -m "feat: POST /cleanup marks old tail entries read (manual escape hatch)"
```

---

### Task 4: PWA 本体（index.html + app.ts）

**Files:**
- Modify: `public/index.html`（完全版）
- Modify: `src/pwa/app.ts`（完全版）

**Interfaces:**
- Consumes: `GET /feed`, `POST /viewed`, `POST /star`, `POST /cleanup`, `ViewedBuffer`
- Produces: 1本リストのトリアージ UI。動作は Task 6 で手動検証。

- [ ] **Step 1: index.html（デザイントークン込み・完全版）**

```html
<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="theme-color" content="#FAF7F5">
<link rel="manifest" href="/manifest.json">
<title>buta</title>
<style>
:root {
  --bg: #FAF7F5; --fg: #23201E; --muted: #8A817C; --accent: #C4526B;
  --hairline: #E4DDD8;
}
@media (prefers-color-scheme: dark) {
  :root { --bg: #1B171B; --fg: #E8E2DE; --muted: #8B8188; --accent: #E27A93; --hairline: #322B31; }
}
* { box-sizing: border-box; }
html { background: var(--bg); }
body {
  margin: 0 auto; max-width: 44rem; padding: 0 0.75rem 40vh;
  font-family: system-ui, sans-serif; color: var(--fg);
  font-size: 15px; line-height: 1.4;
}
header {
  display: flex; align-items: baseline; gap: 0.75rem;
  padding: 0.6rem 0; border-bottom: 1px solid var(--hairline);
  position: sticky; top: 0; background: var(--bg); z-index: 1;
}
header h1 { font-size: 1rem; margin: 0; letter-spacing: 0.04em; }
header .spacer { flex: 1; }
header button {
  font: inherit; font-size: 0.8rem; color: var(--muted);
  background: none; border: 1px solid var(--hairline); border-radius: 4px;
  padding: 0.15rem 0.6rem; cursor: pointer;
}
header button:hover { color: var(--fg); }
#list { margin: 0; padding: 0; list-style: none; }
.row {
  display: flex; gap: 0.6rem; align-items: baseline;
  padding: 0.45rem 0.5rem 0.45rem 0.65rem;
  border-bottom: 1px solid var(--hairline);
  border-left: 3px solid transparent;
}
.row.cursor { border-left-color: var(--accent); background: color-mix(in srgb, var(--accent) 6%, transparent); }
.row.read { opacity: 0.38; }
.row .title { flex: 1; min-width: 0; cursor: pointer; }
.row .title a { color: inherit; text-decoration: none; }
.row .meta {
  font-family: ui-monospace, monospace; font-size: 0.7rem;
  color: var(--muted); white-space: nowrap;
}
.row .star {
  cursor: pointer; color: var(--hairline); background: none; border: none;
  font: inherit; padding: 0 0.2rem;
}
.row .star.on { color: var(--accent); }
.seam {
  display: flex; align-items: center; gap: 0.75rem;
  margin: 1.1rem 0; color: var(--accent);
  font-family: ui-monospace, monospace; font-size: 0.72rem; letter-spacing: 0.08em;
}
.seam::before, .seam::after { content: ""; flex: 1; border-top: 1px solid var(--accent); opacity: 0.45; }
#end { text-align: center; padding: 2rem 0; }
#end button {
  font: inherit; color: var(--bg); background: var(--accent);
  border: none; border-radius: 6px; padding: 0.5rem 1.2rem; cursor: pointer;
}
:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
@media (prefers-reduced-motion: no-preference) {
  .row { transition: opacity 0.4s ease; }
}
</style>
</head>
<body>
<header>
  <h1>buta</h1>
  <div class="spacer"></div>
  <button id="cleanup">掃除</button>
  <button id="reload">更新</button>
</header>
<ul id="list"></ul>
<div id="end" hidden><button id="next">次の尾を回す</button></div>
<script src="/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: app.ts（完全版）**

```ts
// src/pwa/app.ts
import { ViewedBuffer } from "./viewed-buffer";

interface Entry {
  id: number; feed_id: number; title: string | null;
  url: string | null; created_at: string | null;
}
interface Selection { tier1: Entry[]; tier2: Entry[] }

const buffer = new ViewedBuffer();
let rows: HTMLLIElement[] = [];
let cursor = -1;

function token(): string {
  let t = localStorage.getItem("buta_token");
  if (!t) {
    t = prompt("token?") ?? "";
    localStorage.setItem("buta_token", t);
  }
  return t;
}

async function api(path: string, init: RequestInit = {}): Promise<Response> {
  const res = await fetch(path, {
    ...init,
    headers: {
      ...init.headers,
      Authorization: `Bearer ${token()}`,
      ...(init.body ? { "Content-Type": "application/json" } : {}),
    },
  });
  if (res.status === 401) {
    localStorage.removeItem("buta_token");
    throw new Error("unauthorized");
  }
  return res;
}

function fmtDate(iso: string | null): string {
  if (!iso) return "";
  return iso.slice(5, 10).replace("-", "/");
}

const observer = new IntersectionObserver((entries) => {
  for (const e of entries) {
    // Row has scrolled past the top edge -> the user has seen it.
    if (!e.isIntersecting && e.boundingClientRect.bottom < 0) {
      const row = e.target as HTMLLIElement;
      markViewed(row);
    }
  }
}, { rootMargin: "0px" });

function markViewed(row: HTMLLIElement): void {
  const id = Number(row.dataset["id"]);
  if (buffer.add(id)) row.classList.add("read");
}

function makeRow(entry: Entry): HTMLLIElement {
  const li = document.createElement("li");
  li.className = "row";
  li.dataset["id"] = String(entry.id);
  li.dataset["url"] = entry.url ?? "";

  const title = document.createElement("span");
  title.className = "title";
  title.textContent = entry.title || "(no title)";
  title.addEventListener("click", () => openRow(li));

  const meta = document.createElement("span");
  meta.className = "meta";
  meta.textContent = fmtDate(entry.created_at);

  const star = document.createElement("button");
  star.className = "star";
  star.textContent = "★";
  star.setAttribute("aria-label", "star");
  star.addEventListener("click", () => toggleStar(li, star));

  li.append(title, meta, star);
  observer.observe(li);
  return li;
}

function openRow(row: HTMLLIElement): void {
  markViewed(row);
  const url = row.dataset["url"];
  if (url) window.open(url, "_blank", "noopener");
}

async function toggleStar(row: HTMLLIElement, btn: HTMLButtonElement): Promise<void> {
  const starred = !btn.classList.contains("on");
  btn.classList.toggle("on", starred);
  try {
    await api("/star", {
      method: "POST",
      body: JSON.stringify({ entryIds: [Number(row.dataset["id"])], starred }),
    });
  } catch {
    btn.classList.toggle("on", !starred); // revert on failure
  }
}

async function flush(keepalive = false): Promise<void> {
  const ids = buffer.drain();
  if (ids.length === 0) return;
  try {
    await api("/viewed", {
      method: "POST", keepalive,
      body: JSON.stringify({ entryIds: ids }),
    });
  } catch {
    buffer.restore(ids);
  }
}

function setCursor(i: number): void {
  rows[cursor]?.classList.remove("cursor");
  cursor = Math.max(0, Math.min(i, rows.length - 1));
  const row = rows[cursor];
  if (!row) return;
  row.classList.add("cursor");
  row.scrollIntoView({ block: "center" });
  markViewed(row); // moving the cursor onto a row counts as seeing it
}

document.addEventListener("keydown", (e) => {
  if (e.target instanceof HTMLInputElement) return;
  if (e.key === "j") setCursor(cursor + 1);
  else if (e.key === "k") setCursor(cursor - 1);
  else if (e.key === "s" && rows[cursor]) {
    const btn = rows[cursor]!.querySelector<HTMLButtonElement>(".star")!;
    void toggleStar(rows[cursor]!, btn);
  } else if ((e.key === "o" || e.key === "Enter") && rows[cursor]) {
    openRow(rows[cursor]!);
  }
});

async function load(): Promise<void> {
  await flush();
  const res = await api("/feed");
  const sel = (await res.json()) as Selection;

  const list = document.getElementById("list")!;
  list.textContent = "";
  rows = [];
  cursor = -1;

  for (const e of sel.tier1) {
    const row = makeRow(e);
    list.append(row);
    rows.push(row);
  }
  const seam = document.createElement("li");
  seam.className = "seam";
  seam.textContent = "ここから長い尾";
  list.append(seam);
  for (const e of sel.tier2) {
    const row = makeRow(e);
    list.append(row);
    rows.push(row);
  }
  document.getElementById("end")!.hidden = false;
}

document.getElementById("reload")!.addEventListener("click", () => void load());
document.getElementById("next")!.addEventListener("click", () => void load());
document.getElementById("cleanup")!.addEventListener("click", () => {
  const days = Number(prompt("何日以上前の尾を既読にする？", "7"));
  if (!Number.isFinite(days) || days <= 0) return;
  void api("/cleanup", { method: "POST", body: JSON.stringify({ olderThanDays: days }) })
    .then(async (r) => {
      const { read } = (await r.json()) as { read: number };
      alert(`${read} 件を既読にした`);
      return load();
    });
});

setInterval(() => void flush(), 4000);
window.addEventListener("pagehide", () => void flush(true));

void load();
```

- [ ] **Step 3: ビルドと型チェック**

Run: `pnpm build:pwa && pnpm typecheck && pnpm test`
Expected: 全 PASS

- [ ] **Step 4: コミット**

```bash
git add public/index.html src/pwa/app.ts
git commit -m "feat(pwa): single-list triage UI with scroll auto-read and keys"
```

---

### Task 5: manifest + アイコン（インストール可能化）

**Files:**
- Create: `public/manifest.json`
- Create: `public/icon-192.png`, `public/icon-512.png`（スクリプトで生成）

- [ ] **Step 1: manifest.json**

```json
{
  "name": "buta",
  "short_name": "buta",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#FAF7F5",
  "theme_color": "#C4526B",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 2: 単色 PNG アイコンを生成（PIL 不要の純 python）**

```bash
python3 - <<'PY'
import struct, zlib
def png(size, rgb, path):
    row = b"\x00" + bytes(rgb) * size
    raw = row * size
    def chunk(t, d):
        c = t + d
        return struct.pack(">I", len(d)) + c + struct.pack(">I", zlib.crc32(c))
    ihdr = struct.pack(">IIBBBBB", size, size, 8, 2, 0, 0, 0)
    data = b"\x89PNG\r\n\x1a\n" + chunk(b"IHDR", ihdr) + chunk(b"IDAT", zlib.compress(raw)) + chunk(b"IEND", b"")
    open(path, "wb").write(data)
png(192, (196, 82, 107), "public/icon-192.png")
png(512, (196, 82, 107), "public/icon-512.png")
PY
```

- [ ] **Step 3: コミット**

```bash
git add public/manifest.json public/icon-192.png public/icon-512.png
git commit -m "feat(pwa): manifest and icons for installability"
```

---

### Task 6: デプロイと実機トリアージ（手動）

- [ ] **Step 1: デプロイ**

```bash
pnpm deploy
```

- [ ] **Step 2: Mac (ブラウザ) で確認**

`https://buta.yuta25.workers.dev/` を開き、トークンを入力して：
1. Tier 1 → 継ぎ目「ここから長い尾」→ Tier 2 が 1 本のリストで出る
2. スクロールで通過した行が薄くなる（自動既読）
3. j/k でカーソル移動、s でスター（ローズに点灯）、o で原文が新規タブ
4. 数秒後に再読込しても、通過分が未読リストに戻ってこない（Feedbin 反映）
5. 「次の尾を回す」で Tier 2 が新しいフィード群に入れ替わる（回転）
6. 「掃除」で日数を入れると尾の古い未読が既読化される

- [ ] **Step 3: Android (Chrome) で確認**

同 URL を開き、ホーム画面に追加（standalone 起動）。親指スクロールで自動既読が効くこと、スタータップが押しやすいことを確認。

- [ ] **Step 4: チューニングメモ**

体感が悪い箇所（行密度、自動既読の閾値、フラッシュ間隔、尾の総枠30）を DESIGN.md の「残す小論点」に追記して次イテレーションへ。

---

## Self-Review

**1. Spec coverage（DESIGN.md トリアージUX / MVP）:**
- 1本リスト（ユーザー決定）＋密な見出しリスト → Task 4 ✓
- スクロール自動既読（提示分のみ）＋ j/k・明示操作 → Task 4（IntersectionObserver は通過行のみ）✓
- スター → Task 4（既存 `/star`）✓
- 手動掃除の逃避ボタン（尾のみ・自動なし）→ Task 3/4 ✓
- 未読カウント非表示 → Task 4（どこにも出さない）✓
- 原文はブラウザで開く → Task 4（`window.open`）✓
- Android/Mac 両対応＋インストール → Task 5/6 ✓
- online-first → 前提どおり（Service Worker なし）✓
- **非対象**: 記事リーダーモード（Phase 2）、オフライン、学習ランク。

**2. Placeholder scan:** HTML/TS とも完全なコードを記載。曖昧指示なし。

**3. Type consistency:** `Entry`/`Selection` は `/feed` の実レスポンス（id/feed_id/title/url/created_at）と一致。`ViewedBuffer.add/drain/restore` は Task 2 定義どおり Task 4 で消費。`/viewed` の body `{entryIds}`、`/star` の `{entryIds, starred}`、`/cleanup` の `{olderThanDays}` は plan 3 / Task 3 のサーバ実装と一致。

---

## 未解決（この plan の範囲外）

- iOS Safari は要件外（要件は Mac + Android）。
- 自動既読の発火条件（上端通過）は初期値。実使用で「速すぎ/遅すぎ」が出たら rootMargin を調整。
- Service Worker（オフラインキャッシュ）は非目標のまま。manifest のみで install は可能。
